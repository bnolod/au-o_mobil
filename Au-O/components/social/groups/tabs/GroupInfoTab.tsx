import { View } from 'react-native';
import ThemedText from '@/components/ui/ThemedText';
import { GroupTabProps } from './props';

export default function GroupInfoTab({ group, language }: GroupTabProps) {
  return (
    <View>
      <ThemedText></ThemedText>
    </View>
  );
}
